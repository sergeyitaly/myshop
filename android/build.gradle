buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Classpath for Gradle plugin to build Android projects
        classpath 'com.android.tools.build:gradle:7.4.2'  
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.0' 
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
