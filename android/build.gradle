plugins {
    // This is the Android Gradle plugin
    id 'com.android.application' version '8.3.0' apply false

    id 'org.jetbrains.kotlin.android' version '1.9.0' apply false
}

allprojects {
    repositories {
        google() // Required for Android plugins
        mavenCentral()
    }
}
